# Active Sprint Context

## Current Micro-Task
- Fix council year separation in `lib/data.js` and dynamic default year in `components/teamMembers.jsx`.

## Target File Paths & Mutation Keys
- Target Files:
  - `lib/data.js` -> `teamMembersByYear["2026-27"]`, `teamMembersByYear["2025-26"]`
  - `components/teamMembers.jsx` -> `selectedYear` initialization

## Next Operational Action
- Verification complete. Ready for user testing.
